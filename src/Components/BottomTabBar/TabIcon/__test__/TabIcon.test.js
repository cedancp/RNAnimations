import React from 'react';
import {render} from 'react-native-testing-library';
import TabIcon from '../TabIcon';

describe('Tab Icon', () => {
  it('should render correctly tab icon', () => {
    render(<TabIcon />);
  });
});
