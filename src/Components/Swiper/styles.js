import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cardsContainer: {
    paddingBottom: 75,
  },
  dots: {
    flexDirection: 'row',
    alignSelf: 'center',
  },
  dot: {
    marginRight: 18,
  },
});

export default styles;
